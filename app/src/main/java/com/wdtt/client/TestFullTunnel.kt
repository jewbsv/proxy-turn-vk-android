package com.wdtt.client

/**
 * Тестовый флаг: если true, весь трафик ВСЕГДА идёт через туннель — и
 * фильтр приложений (БС/ЧС), и bypass routes (обход по IP/сайтам)
 * полностью игнорируются в обоих транспортах (Raw и WireGuard). Нужен
 * для тестовых сборок, чтобы исключить "трафик идёт мимо туннеля из-за
 * настроек исключений" как причину при разборе логов от тестеров.
 *
 * Перед релизной сборкой должно быть false.
 */
const val FORCE_FULL_TUNNEL = false
